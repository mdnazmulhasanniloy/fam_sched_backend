 ai:
    image: nazmulhasn/any_day_app_ai:latest
    container_name: any_day_app_ai
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '2007:2007'
    depends_on:
      mongo:
        condition: service_started
    networks:
      - any_day_app_net
